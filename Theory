Learn:-
1)How many number of trainable parameters are there in the neural network model(how many weights and biases are there to be trained)
2)How to denote those weights and biases so that we dont get confused by seeing it that of which layer is that weight

